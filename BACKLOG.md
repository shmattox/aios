---
type: backlog
project: aios
status: in-progress
created: 2026-07-02
tags: [backlog, b2g, aios, plugin, engine, go-forward]
---

# AIOS Plugin — Go-Forward Backlog (the engine SoT)

> **This is the single source of truth for AIOS *engine/plugin* work.** Non-engine **env/vault**
> work lives in the env-ops backlog (`Documents/Claude/BACKLOG.md`); when they disagree about
> engine work, THIS file wins. Product framing: `SecondBrain/03_GeneralManagement/wiki/entities/aios.md`.
> **The boundary test:** only universally viable engine/product work lives here; anything unique
> to Seth's instance belongs in env-ops or the owning ritual — a moved item leaves a pointer.

**Format:** uniform B2G items — `- [ ] **A#** — headline (markers)`, descriptive sub-bullets, an
`acceptance:` line phrased as a command run-and-shown. Feed to **backlog-to-goals**. `[GATE: human]`
items need Seth's hands. **ORDER IS PRIORITY** among open items; re-rank in place with a dated note.
**On close (2026-07-05 convention):** compress the item to ONE line under `## Done` — the full
close-out paragraph lives in the closing commit (git history is the archive); validation tails go
to `## Watching`, and a Watching line is DELETED once observed clean (or promoted to a new item if
it fails).

**State (2026-07-12):** **v0.4.1 tagged + public** (A59 brief thread-reconciliation) — released on a **clean-root history** (A61): the public repo was reset to a single root commit and the old `v0.1.0–v0.4.0` tags deleted, scrubbing a personal install's operational identifiers (entity names / open-item ids / synthetic slugs) that had leaked into test fixtures + docs + history; the pre-tag `sanitize_check` guard now blocks recurrence. (A prior 2026-07-07 pass scrubbed economic figures / Notion ids / Drive refs via git-filter-repo.) **Laptop + any other clone must delete-and-re-clone** (rewritten root — a reset won't fix it). The resolve layer FIRES in the brief (A31: worklist read + `render_dossier` + the anti-skip `check`), an opt-in overnight `aios-resolve-sweep` warms the cache (A34), `/aios:setup` seeds `resolve.*` defaults (A33). v0.3.0 shipped the resolution tools before that; v0.2.0 before it. Single-file queue, deterministic stage tools,
deduped deploy bodies, live-vault garden, Notion-live headless brief, integrity-checked runners,
Windows+Mac+Linux scheduling. Installed plugins are pinned snapshots — distribute via a new tag +
`/plugin update aios`.

---

## Watching — validation tails (check, then delete the line)
- **A73 tails:** (1) first UNATTENDED standup tick (factory 20-min/nightly) writes `standup.json` with the `acceptance` block and the next real brief renders the two 📊 lines + the gate_metrics render line (UTF-8 clean, no mojibake); (2) gate `$/accepted` currently renders as absent (loud-unavailable) because the H62 task-log spend ledgers haven't had a confirmed live fire — it fills in when H62's Watching item clears; (3) 71 factory Done-lines lack closing SHAs (`unknown_sha`) — improves as the Done convention carries SHAs; backfill optional. Delete each once observed.
- **A73 residual notes (final review, non-blocking):** `_within` closure ×3 + double backlog-parse in `factory_standup.py` (future dedup, perf immaterial at daily cadence); the Dev-scope standup empty-state line is now effectively unreachable (acceptance block always truthy); future trim = fold $/accepted into `gate_metrics render` and drop the panel's gate line (the two gate-acceptance surfaces are intentionally BOTH kept for v1 — render carries decider mix + override ids, panel carries $/accepted).
- **[Seth] `/plugin update aios` → v0.5.0 to go live:** A70 (brief header live-synthesis) ships in **v0.5.0**; the active install is an older pinned snapshot, so run `/plugin update aios`, then fire "Wake up. Daddy's home." and confirm the header is a static loading ack + a this-run-fresh paragraph (no precomputed `state/brief-headline.md`). Delete once confirmed live.
- **[Seth] 2026-07-14 aios history reset AGAIN — re-clone everywhere:** the public `aios` repo was reset to a NEW clean-root + `v0.5.0` and `v0.4.1` deleted (A71 privacy scrub — the A61 pass had missed a real EIN + GM/recent entity names). Supersedes the 2026-07-12 reset. The **laptop** + **any other clone** have a divergent old root — `git reset` will NOT reconcile a rewritten root; **delete and re-clone** from the latest. Delete this line once the laptop is re-cloned.
- **[Seth] copy the sanitize name-file to the laptop:** `<env_root>/state/sanitize-patterns.txt` is created + seeded with the full FO roster (gitignored/local — it won't sync). Copy it by hand to the laptop so the pre-tag guard's name tier works there too. Delete once copied.
- **[Seth] Relay goal test-path typo — FIXED 2026-07-12, propagate to laptop:** relay `/goal` strings for aios were compiled with `python -m pytest engine/tests -q` (nonexistent → exit 4), so the goals couldn't auto-clear even though the work shipped clean (A62 45/45; A65 47/47, review-gate PASS, `c70d382`). Root cause: the model guessing the aios test path when invoking `backlog-to-goals`. **Fixed** by adding an explicit AIOS test-path note to `~/.claude/skills/backlog-to-goals/SKILL.md` (correct path = `engine/tools/tests`; a `conftest.py` bridges the `check()`-harness; prefer the pathless `python -m pytest -q` when unsure). Residuals: (a) `~/.claude` is NOT git-synced → **propagate the SKILL.md edit to the laptop** (via `installed-skills` reconcile or manual copy); (b) the two stranded A62/A65 relay `/goal` strings auto-clear when their windows close (no action). Delete this line once the laptop copy is propagated.
- **[Seth] A59 — confirm the 0.4.1 brief routes threads live:** next real "Wake up" brief should put a parked awaiting-send item → ⏳ In-motion, an open notice-response item → Act (reframed to its concrete next action); a synthetic-id item links only if the gather sets its `thread_id` (no auto-join). Delete once observed clean.
- **[Seth] A56's ~7 genuine-gap knowledge recoveries (optional, low value):** the backfill found ~7 real operational concepts H39 retired that aren't yet in `knowledge/` (mostly foldable as subsections into `agent-harness.md` ×4 + `loop-engineering.md` ×1 + `second-brain-pkm-design.md`; 2 new pages `agents-md-convention`, `ai-video-production-pipeline`). Listed in `docs/superpowers/findings/2026-07-10-a56-backfill-classification.md` §Genuine-gap. Recover by hand if/when wanted (~1-2 units), else delete this line — they're preserved in the findings doc + the raw archive.
- **[Seth] 9 unevidenced missing stubs (A28 residual):** `personal/wiki/sources/` items gone with
  NO archived husk and no same-slug knowledge page (luma-society-account-security + 8 X-captures
  of 2026-06-21, ids in the A28 close-out commit `4580be5`). Reconcile flags them until
  dispositioned: if they were distilled under different names / deliberately deleted →
  `rewind.py mark-retired <queue> <ids> "<why>"`; if genuinely lost → let a `reconcile --apply`
  rewind + redraft them. Blanket live `reconcile --apply` stays parked until this line clears.
- **Next Sunday garden (03:10):** context-log `audit` totals (orphans/dead links) trending toward
  ~0 from the 07-05 baseline of 26/96 (garden_audit steady-state watch).
- **Next Sunday garden (03:10), first rulebook run (A3):** hygiene-oracle proposals enqueued with
  correct tiers — NO semantic item on `lane: auto-ship` (queue + context-log check); mechanical
  fixes on dev/personal ship via gate-auto, FO holds.
- **[A54 review MEDIUM] deduped-session downstream reachability:** after A54's session dedupe stops
  the abort, the deduped queue item still points at its OLD `payload_path` (the file was re-discovered
  at a moved path — that's why it collided). Capture is additive-only, so it heals the ledger fence but
  not the queue pointer. Confirm the H26-moved sessions now ingest cleanly downstream (sort/ingest read
  `payload_path`) — the live queue-path repair is env-ops H26 reconcile (`9a23e1e`). If any deduped
  session orphans in sort/ingest on a stale `payload_path`, promote to an aios item (reconcile that
  repairs the queue pointer, or make sort/ingest tolerate+heal a moved payload_path).
- **[A12 follow-up] git-coupled prose + guard-test hardening (final-review Minors, non-blocking):**
  (a) `skills/gate/SKILL.md:63` ("Native git only… the native auto-sync commits") still couples ship→commit
  without the "git optional" caveat the rest of A12 now carries — it's an instance/sandbox `state/`-auto-sync
  note, non-contradictory, but soften it to match. (b) `test_a12_non_git_vault.py`: on Windows the marker-based
  `guard:` check is inert for bare `shell=False` git (the bite there is `FileNotFoundError`), so a future git
  call that try/excepts+swallows would evade the guard ON WINDOWS ONLY (POSIX marker always catches) — wrap the
  ship/undo calls and convert any exception to an explicit guard FAIL to close it. Delete this line once (a)+(b)
  land or are judged not worth it.
- **[A6 deferred] live-Linux scheduling round-trip unverified (no Linux box):** Mac launchd is live-round-trip-verified;
  Linux `deploy/linux/` crontab registrar/unregistrar/runner are BUILT + reviewed (`1d2c248`) but never fired on a real
  Linux box. A6 closed 2026-07-10 (won't get hardware now). If a Linux user/box ever appears: run
  register→fire→unregister on a throwaway clone (the double-run caveat — a 2nd machine sharing this env's state/vault
  would double-run) and confirm. Delete this line once done, or if Linux support is dropped outright.

## Open (order is priority)

_(2026-07-15 re-rank — **A72 placed above A69**: A72 blocks the aios factory lane entirely (a red suite makes every aios drain refuse to ship), so it gates A69's own autonomous delivery. Re-rank as you see fit.)_

_(2026-07-15 — **A78 added directly above A72**: A78 delivers A72's fix as one of its legs (the frozen-golden replacement), so A72 cannot ship independently of it. A72 is retained as the separately-verifiable acceptance, not as parallel work.)_

- [ ] **A78** — **`domain_sync.py` — the missing Notion→local pipe (state-consolidation #5, engine leg).** Spec `<env>/docs/superpowers/specs/2026-07-15-domain-sync-notion-to-local-design.md` (design-approved rev c, 2026-07-15; ecosystem-check verdict `adapt-skill`) · **Plan 1 (foundation) — SHIPPED 2026-07-15** `<env>/docs/superpowers/plans/2026-07-15-domain-sync-plan-1-foundation.md` — delivered A72's fix + the S8 change (both ticked ✅ below); Plans 2 (FO mapping) and 3 (the pipe) unwritten. *(promoted from the 2026-07-15 brainstorm; placed at top — it is the prerequisite of #4 (A63 dashboard) and #3 (H19 flip), and it carries A72's fix.)* The mirror has ~480 records, **zero code readers**, and no sync in either direction — `domain_mirror` is import-only, fed by hand-made 06-30/07-01 exports, so it has been frozen and drifting for two weeks while a human (CHG-97) authored economic content *upward* in the opposite direction from the README's stated contract. Build the seam, not a subsystem: the fetch half already exists (`notion_gather.py` — headless-proven token resolve, `normalize_id`/`normalize_page`/`_query_source`) and `domain_mirror.import_silo` already maps-and-emits. New code is confined to the thin differentiator — a table walker over each silo's `schema.yaml`, a **committed `_slugmap.json`** (first-seen-wins; a title change NEVER re-slugs — the exemplar loan-note file keeps its stale-looking name deliberately, the slug is an identifier not a summary), a **guarded reap** (archive-not-delete to `_retired/<date>/`; skip reap entirely for the silo on ANY degraded/partial fetch — the A49 rot lesson; volume brake fails loud), and a `sync-status.json` sidecar reusing **A60's exact `sweep-status.json` pattern** (no sixth observability surface — H51). **✅ Plan 1 (2026-07-15) — S8 (hard prereq): one-line change — `import_silo` writes `out / table["source_db"]` not `out / table["name"]`** — `source_db` already IS the semantic name in all three silos, so an unmodified sync would create `state-note/` beside FO's `notes/` and silently duplicate every record. **S9: GM is EXCLUDED** — its schema header declares it local-SSOT with publish-OUT (no `source_db`/`notion_fields` at all); applying the sync to it would destroy its 32 factory-authored records (37 `.md` files on disk, 5 of which are `README.md` and validator-excluded — corrected 2026-07-15, C5). The env runs two opposite models by design — do NOT "unify" them. **S10: reap is scoped to schema-MAPPED tables only** (FO has 13 on-disk dirs, 5 mapped; an unscoped reap would archive the other eight). **✅ Plan 1 (2026-07-15) — carries A72's fix:** replaced the live-vs-frozen comparison with `import(frozen 06-30 export) == frozen golden` extracted from commit `81aaf14` (verified pre-drift), which preserves A53's real-data field-mapping proof (162 records) a plain deletion would discard, and lifted the A53 `_has_multiline_scalar` skip (one real note, now compared — notes 118/118). See A72's Done-line for the shipped detail.
  - acceptance: `python -m pytest -q` in `Projects/aios` exits **0**, full suite green (shown), with the A72 live comparison replaced by the hermetic frozen-golden regression and the `_has_multiline_scalar` skip lifted so the one real note it excluded is field-compared (notes 118/118 — NOT the john-doe asset, which stays unmapped until Plan 2); `python Scripts/factory-gate/factory_gate.py --json` shows `Projects/aios` gated by a green `test_cmd` (shown); a Personal import lands in `tables/tasks/` not `tables/state-task/` and `state_validate --all state/domains/personal` PASSes (shown); a **reap-scope** test proves reap never touches a directory no mapped table owns (S10), using a fixture silo with a deliberately unmapped dir; a **simulated partial-fetch** run skips reap entirely and marks the silo degraded rather than archiving records (the guard proven, not assumed); the sync run logs GM as explicitly skipped with the local-SSOT reason (S9) rather than silently omitted; a fresh-context review subagent (not the builder) reports zero CRITICAL in chat.

- [ ] **A80** — **`state_native:` — stop the importer deleting fields no rule can reproduce.** Spec `docs/superpowers/specs/2026-07-15-a80-state-native-design.md` (design-approved 2026-07-15; ecosystem-check verdict `adapt-skill`). *(promoted from seed 2026-07-15 — brainstormed with Seth, spec approved same session; placed ABOVE A78 because **A78's own Plan 2b and Plan 3 both block on it**: until this ships, a sync deletes locally-authored data on contact.)* `build_record` rebuilds frontmatter from scratch, so an undeclared field isn't overwritten by the source — it **ceases to exist**; 18 values on already-mapped tables would go on the next sync, and the only prior record of the class is a test-exclusion set. **One optional schema key, three touch points, no new module:** `load_silo_config` reads `state_native:` (+ fails loud at load on a field declared BOTH state_native and derived); `import_silo` reads the declared keys off the destination record; `build_record` takes an optional `preserved` dict and emits it **in the computed slot** — exactly where the field already sits on disk (index 21 of 24), so the first sync shows **zero diff**. Absent → **omitted, never null** (14 of 21 records on one table have no such key). Unreadable destination → fail loud. Scope is bounded by ONE test — *can any rule reproduce it?* — which splits the old exclusion set cleanly: `wiki` is state_native; `owner_entity` (a ruleset derives it) + `asset` (a decode) are **Plan 2b**; generated view blocks are **A81**. **Fields only, never bodies.** Fact-free: the list lives in the silo schema. `state_validate` needs no change (its `relations:` list already covers the type axis).
  - acceptance: `wiki` compared **18/18** against the frozen golden (shown) with `_CURATED` reduced to `{owner_entity, asset}` — the exclusion that hid this class becomes coverage; `a72_regression_is_hermetic` still green (seeded from the **golden**, never the live tree); a fixture silo proves absent→omitted / no-destination→omitted / collision→`ValueError` before any write / a state_native field survives a re-import while its `notion_fields` siblings update; a real import produces **zero git diff** on the 18 records (the byte-order proof); a table declaring no `state_native:` is byte-identical to before (regression shown); `python -m pytest -q` exits 0 (shown); `state_validate --all` PASSes all three silos (shown); a fresh-context review subagent (not the builder) reports zero CRITICAL.

- [ ] **A69** — **Reflect Lessons pass can't enqueue — queue `conflict_key` is vault-wiki-only (found in the 2026-07-12 reflect dry-run). BLOCKS enabling `aios-reflect`.** Reflect's headline differentiator — the daily self-learning loop that proposes a rule into the owning `CLAUDE.md` "Lessons" block or a `Memory` entry — produces draftless proposals whose target lives OUTSIDE the vault wiki (the env-root `CLAUDE.md`, `~/.claude`, `Memory/`). Those fail `queue_tx`'s `conflict_key` shape guard (`<kb>/wiki/<type>/<slug>`) and the whole atomic change-set is rejected (confirmed live in the dry-run: the journal/knowledge/decisions passes enqueue fine; the Lessons pass cannot). So reflect-as-built distills everything EXCEPT the reason it was built. **Chosen approach (a):** (1) relax `queue_tx` conflict_key validation to accept a non-wiki lessons key shape (e.g. `env/lessons/<slug>`), keeping the wiki shape as the default + only-allowed for `draft_path` items; (2) teach the gate / `ship.py` to apply a draftless CLAUDE.md/Memory proposed-diff to its target file (anchor-line insert; hold-and-flag if the anchor moved). Alternatives rejected in the dry-run report: (b) route lessons through a non-queue review file (bypasses the gate); (c) downgrade lessons to vault `knowledge/lessons-*` pages (loses the exact-diff value). Brainstorm→spec (touches the queue conflict_key CONTRACT + the gate apply-target surface — load-bearing; a moved-anchor + Paper-Governs check belong in the spec). Note: the fresh-context pre-enable review MISSED this (it confirmed lessons were draftless+gated but didn't trace the conflict_key rejection); the real-data dry-run caught it — evidence for keeping the dry-run gate.
  - acceptance: a reflect Lessons proposal (draftless, env-root `CLAUDE.md` target) enqueues via `queue_tx add` (shown), and `/aios:gate` applies its one-line diff to the `CLAUDE.md` Lessons block — or holds-and-flags on a moved anchor — demonstrated on a fixture; the existing vault-wiki `conflict_key` path is unchanged (regression shown).
---

## Next — seeds (headline-only; brainstorm to promote into Open with spec + acceptance)
- ▶ **A80** — **PROMOTED to Open** (spec `2026-07-15-a80-state-native-design.md`, 2026-07-15). See Open above.
- ◷ **A81 — the mirror is not import-only: a second generator appends body content, and the sync would strip it.** Every record in one mapped table carries ~2.2KB of generated DataviewJS relational views — appended *after* import by a separate renderer, under a marker that says "not part of the mirror" (~24KB across 11 records). A78's architecture is `gather → snapshot → import → reap → validate → commit` — **no render stage** — so every sync would strip them and the next render would put them back: pure churn drowning the git-diffable audit trail that justifies the whole sub-project. Decide: chain the renderer as a post-import stage, or make the importer body-preserving (relates to A80 — same disease, body instead of fields). Brainstorm→spec. Origin: `<env>/docs/superpowers/findings/2026-07-15-plan2-blockers-and-the-state-native-gap.md` (Finding 4b), found while planning A78 Plan 2.
- ◷ **A63 — Dashboard over the state engine (state-consolidation #4).** Read-only render layer over `state/domains/<silo>/tables` + the repo `BACKLOG.md` set + brief/factory caches — the visual sibling of the brief, and the SSOT flip's (H19) parallel-run safety net; build it BEFORE the flip. Brainstorm→spec required; the §Ecosystem-check leg 4 genuinely matters here (Obsidian Bases / Notion-as-viewer / off-the-shelf dashboard tooling vs custom — feed the buy-vs-build with `deep-research`, don't reason from memory). Blocked-by: env-ops **H57** normalization (silo table-dir naming split, records-under-tables contract, legacy queue rows). Instance wiring = env-ops **H61**.
- ◷ **A64 — Local-first write path for domain records (H19 prerequisite, state-consolidation #3 engine leg).** `domain_mirror` is import-only today; the per-silo SSOT flip needs a `queue_tx`-style transaction for `state/domains` records (atomic, validated, revertible) + a gate lane for economic/Paper-Governs writes + Notion write-back becoming the *derived* direction. Full gate on the spec (Paper-Governs adjacent). Without this, "drop Notion" has no write story.
- ◷ **A66 — "Missing-link-between-mature-clusters" reflection pass (F8 extension).** Distinct from orphan detection (a page with *no* links) and from F8.4 (theme *convergence*): hunt for **two well-developed clusters that clearly belong beside each other but never touch** — the "I wrote a lot about X and a lot about Y and never connected them" gap. Proposal = the bridging wikilink(s) or a bridge/MOC page, `lane: review`. Pairs naturally with A65 (embedding distance between cluster centroids is the natural detector). Brainstorm→spec. (Same benchmark source as A65.)
- ◷ **A68 — Gate stamps `explored: true` on ship (+ backfill).** `ship.py` moves a draft to its
  canonical folder but never flips `explored:`, so every pipeline-shipped page reads `explored: false`
  — the field is dead for in-vault Dataview. Have `ship.py` set `explored: true` when it writes the
  canonical page (not for daily-note merges — those aren't "explored"), plus a one-time backfill sweep
  of already-shipped pages currently stuck `false`. Small, well-scoped; makes the gate-managed field
  (front-door decision 2026-07-12) actually work. Surfaced during env-ops Phase 0 (`docs/superpowers/specs/2026-07-12-brief-as-front-door-design.md`).
- ◷ **A67 — Live in-vault Dataview health dashboards.** Dataview is installed but unused in live wiki pages; `garden_audit` + the chat brief cover graph health *when a session is open*. A few Dataview dashboard notes (orphans · created-this-week · growing-clusters · isolated-too-long) give an at-a-glance view **inside Obsidian with no Claude session** — complements the brief, doesn't compete. Lowest priority of the three; deterministic-render caveat applies (the must-hold queries are engine-rendered, not hand-authored prose). Brainstorm-lite→spec. (Same benchmark source as A65.)
- ◷ **Durable synthetic-id thread linkage (A59 follow-up).** The A59 thread-join is deterministic for numbered item ids but synthetic-id Act items link only when the gather sets `thread_id` by judgment each run — not durable across unattended gathers (proven 2026-07-11: a synthetic-id item went cold until hand-set). Brainstorm the airtight path: give synthetic items real numbered ids · thread-declared `serves:` keys · a safe entity/conflict_key match (reviewer flagged fuzzy-title false-positive risk) · or a deterministic pre-pass the gather confirms. Also fold in the court-hint finding: court is driven by thread `status`, so a "waiting on others" item whose thread is left `open` shows in Act — wants a thread-hygiene nudge or a next_action-aware court hint.
- ▶ **A73** — **PROMOTED to Open** (spec `2026-07-15-a73-gate-acceptance-metrics-design.md`, 2026-07-15). See Open above.
- ◷ **A74 — Earned-autonomy lane graduation (A41 REOPENED, advisory form — gated on A73 data).** A41 closed won't-build 2026-07-10 with the trigger "reopen on operating evidence that the static opt-in is real friction"; Seth named the review gate his main bottleneck 2026-07-15 (loop-engineering review), which fires it — **but honestly check the volume case at brainstorm:** steady-state review inflow measured ~1/day and mostly legitimate (2026-07-14), so the win may be small outside priming humps; let A73's real acceptance data decide. Design stays A41's advisory form: a class with N≥20 decisions, ~100% approval (and, once A75 exists, `paper_evidence: matches` throughout) → the system files a `source:self` proposal to graduate that class review→confirm-TTL (post-hoc veto window); one reject auto-demotes. Human ratchets, never system self-widen; the FO kb backstop is NEVER graduation-eligible. Brainstorm→spec. Origin: findings item 2 + Cat Wu's dead-gate warning.
- ◷ **A75 — Paper-evidence verifier leg for Paper-Governs holds (evidence-linked approval packets).** The highest-value gate item: economic/ownership holds exist because the claim must be checked against an executed Drive doc — a read the agent can run (Drive MCP / dataroom-ingest projections) but that Seth performs by hand per item today. Add a verifier leg (ingest enrich or a gate pre-pass) that resolves each Paper-Governs hold against the paper and attaches `paper_evidence: {doc, section, quote, verdict: matches|conflicts|no-paper-found}`; the brief hold card renders it, so Seth's per-item cost drops from "find and read the paper" to "does this citation check out — approve/veto." `no-paper-found` stays fully manual (the honest residual; the tripwire already knows this class). Karpathy's line applies literally: this class is held not because it's unverifiable but because the verifier isn't pointed at it. Brainstorm→spec (Paper-Governs adjacent — full gate on the spec; evidence is advisory input to Seth's judgment, never an auto-ship signal by itself). Origin: findings item 1.
- ◷ **A76 — Decision-similarity batching in the brief walk.** Group decision-lane holds by shared entity/paper (all items on one counterparty together) so one paper-read amortizes across several approvals; today the walk batches hygiene-vs-decision but orders decisions by arrival. Cheap render-layer change (`brief_render`/walk seeding); brainstorm-lite. Origin: findings item 4.
- ◷ **A77 — Brainstorm-packet questions as brief decision cards (render leg; pairs with GM19).** When GM19's pre-brainstorm harness emits a packet (recommended design + the 3-5 genuinely-Seth questions with proposed defaults), render the pending questions as brief decision cards via the standard AskUserQuestion affordance — all Seth-judgment converges on the one front door (2026-07-12 decision) instead of requiring a dedicated sit-down brainstorm session. Engine leg only; the packet producer is GM19 (factory mechanics). Blocked-by: GM19 defining the packet contract. Origin: findings item 6.
- ◷ **A79 — sanitize_check scans BACKLOG.md only.** The wired gate misses `docs/superpowers/**` and `.superpowers/sdd/`, which are equally public. Widen the scanned set (fail-open, same posture). Found while planning domain-sync Plan 1: the very token Task 0 scrubbed from this file still sits in three unscanned tracked files. **Note for whoever builds this:** state the example obliquely — writing the literal token into this backlog re-trips the guard, which is exactly what happened on the first attempt at this seed (caught by the gate, as designed). A widened scanner must also not flag its own item text.
- ◷ **Capture worthiness floor (reduce low-signal KB drafting).** Ingest currently drafts a full KB page from nearly every sorted economic/social item, so trivia lands on the review lane and accrues (a **$22.30 vendor charge** + a $12.28 failed charge became standalone KB pages; "single tertiary X post" auto-drafts a Personal page). Add a proportionate **worthiness floor** at sort/ingest: sub-$N standalone economic events and tertiary social bookmarks route to a lighter disposition (drop, or a low-priority `reference` bucket) instead of a full `review`-lane draft — the genuine Paper-Governs material (deal paper, term sheets, lending events) is untouched and still held. NOTE: verified 2026-07-14 that steady-state review inflow is already only ~1/day and *mostly legitimate* — this is a minor trim, not urgent; the felt "flood" was a one-time KB-priming hump (52 of 68 held items dated 05-31/07-03/07-05), now being drained. Brainstorm→spec; §Ecosystem-check leg on whether Sort's existing classifier already exposes a value/tier signal to gate on. (Env-ops origin: 2026-07-14 brief-review cleanup session.)

---

## Done — one line per item; full close-outs live in this file's git history (the closing commits)
- [x] **A73** — Gate + factory acceptance metrics ($/accepted-result): `gate_metrics.py` (outcome × decider × recommendation-agreement × window over queue terminal items, report/render CLI) + `ship.py` `decided_by` at flip + standup panel acceptance lines + brief SKILL wiring; env leg `factory_standup.collect_acceptance` (Done-line SHA revert scan + spend joins, B1/B2 loud-unavailable + UTF-8 fixes). Live: n=1146 (811/335/0), 79 overrides surfaced, factory $0.47/accepted. SDD 5 tasks + opus final review SHIP-WITH-NOTES (2 real blockers caught+fixed; Q1: both gate lines stay — render carries decider/override detail, panel carries $/accepted). aios `0f11660`..`e957c4c`, env `99f1c8c`+`f3be998`. Spec/plan `docs/superpowers/{specs,plans}/2026-07-15-a73-gate-acceptance-metrics*` (✅ 2026-07-15)
- [x] **A72** — FO data-drift check separated from the code suite: the live comparison was meaningless by construction under the derived-mirror contract, so both sides are now frozen (golden@81aaf14, private FO repo); real-data field-mapping coverage preserved, notes 118/118 with the vestigial multiline skip lifted. Suite green. (✅ 2026-07-15, `8d8d018`)
- [x] **A71** — Public-repo residue audit + guard hardening (A61 follow-on): `sanitize_check` run tree-wide surfaced real residue A61's FamilyOffice-only pass missed — a GM entity, a tax id (EIN), a first name, two more entities. Scrubbed all → distinct synthetics across the brief-render/threads fixtures + the 2026-07-11 spec; hardened the guard with an EIN structural pattern (leading `(?<!\d)` catches a label-glued EIN, trailing `\b` rejects filename-hash false-positives) + 6 tests; seeded the sensitive terms into the gitignored patterns file only and rewrote the A71 item oblique. review-gate PASS (LOW notes applied — incl. a self-caught real-EIN re-leak in a code comment + a 3-lens synthetic-name collision). Guard tree-wide name-tier 0 + EIN 0. **Released v0.5.0 via history purge** (single-root reset + `v0.4.1` deleted + force-push, Seth's call — laptop re-clone required; the prior A61 root had the miss). ✅ 2026-07-14
- [x] **A70** — Brief header live-synthesis: retired the precomputed `state/brief-headline.md` preview blurb (chronically stale — its at-desk write-back never fired, so it echoed a days-old paragraph each run). Deleted the artifact + the two-pass instant-paint machinery (SKILL `# ⚡ FIRST`, PASS 1/2 render flow) + the latent `brief_freshness_check.py` monitor and its `run-task.ps1` call site. First output is now a static loading ack; the valued header synthesis (masthead + `headline_bubbles` chips + narrative + the 3 deterministic health lines) moved to **render-time**, generated from the exact data each brief shows → cannot drift. `brief-cache.json` untouched. SDD 5 tasks + opus final review (caught 2 gather.md sweep gaps: dropped four-count chip spec + retired PASS-2 vocabulary — both fixed); 48 pytest green; both repos pushed. **LIVE in v0.5.0** (2026-07-14; the pre-tag guard surfaced a real-residue leak → A71, resolved, then released together). Spec/plan `docs/superpowers/{specs,plans}/2026-07-14-brief-header-live-synthesis*`. ✅ 2026-07-14
- [x] **A61** — Public-repo release + privacy hygiene: shipped **v0.4.1** (tag + push) on a **clean-root history**. The fresh-context review-gate caught that A61-as-filed under-scoped the leak — real FO names/ids/slugs pervaded ~15 current-tree fixtures + design docs + ~28 commits of history, not just BACKLOG. Scrubbed the whole current tree to synthetic sample data (entity names, open-item ids, silo slugs, and the notice code all mapped to generic placeholders, consistently so the suite stayed 49/49), reset history to one clean root + deleted the old leaky `v0.1.0`–`v0.4.0` tags (fresh-clone leak scan clean; no reachable ref exposes old tokens). De-Coworked `RESOLVE-INSTALL.md`; header pointers current. New pre-tag guard `engine/tools/sanitize_check.py` (structural OI-/FO- id-format patterns public + out-of-repo instance name-file so names never re-leak) + 17 tests; review-gate PASS + fixed its HIGH self-leak note + 3 MEDIUM guard findings. **Two-pass:** seeding the guard with the full FamilyOffice roster then caught four FO entity/trust/LLC names the first scrub's partial denylist had missed — still live in the tree → a second full-roster scrub + re-squash; a whole-tree guard scan (name tier) is now clean (structural tier only flags synthetic fixture ids). Lesson: run the guard tree-wide as the acceptance, not a hand-picked grep on BACKLOG. Laptop must re-clone from the latest root. (✅ 2026-07-12)
- [x] **A65** — Embedding-based semantic connection discovery: new read-only `garden_neighbors.py` oracle (local/offline `fastembed`, lazy-imported → stdlib-only import + fail-soft SKIP exit-0 when the dep/model is absent) emits within-KB nearest-by-meaning candidates for orphan/weakly-linked pages; `garden_audit` refactored to expose `walk_pages` + additive `inbound`/`adjacency` (backward-compat, regression-proven); `rulebook/passes-semantic-connect.md` (SEMANTIC tier, `lane: review` only — embeddings are input to judgment, never an auto-link) wired into SKILL.md step 1 + `deploy/tasks/garden.md`; garden-scoped `requirements-garden.txt` (engine's first 3rd-party dep, contained) + gitignored cache `state/garden/embeddings/<kb>.json`. TDD via the 7-task plan; fresh-context review-gate PASS (0 confirmed blockers, folded in 3 cheap findings: `_arg` valueless-flag robustness + CLI SKIP-contract + stem-collision adjacency tests). Suite 47 green. ✅ 2026-07-11 (auto: local semantic connect oracle — VETO)
- [x] **A62** — `queue.json.last-good` DR-copy claim resolved as **option 2 (design changed, not a lost write)**: A4 (2026-07-05) deliberately moved recovery to git (`state/` tracked) + rewind snapshots — the engine's own `QUEUE.md` states "no derived mirror or backup copy", `garden_sweep` *deletes* `*.last-good` as legacy residue, and the "no second copy" premise was false (git commits + rewind snapshots are the copies). Writing a `.last-good` would contradict the engine + be garbage-collected by garden. Fix = corrected the stale env `state/README.md` bullet (env repo) to document the real recovery story (atomic write + fail-loud + git + rewind); no engine change (gold-plating against an explicit design). Engine suite 45 green (unchanged); fresh-context reviewer PASS (doc now accurate + self-consistent with QUEUE.md). ✅ 2026-07-11 (auto: env README recovery-story truth-fix — VETO)
- [x] **A60** — Resolve steady-state demotion (brief stops crying wolf on a known-stable backlog): new `_candidates_fingerprint` (id + sorted candidate refs — stable across the title churn that made `content_hash` useless, flips on any new/resolved task or changed candidate) + a `candidates_unchanged_days` counter in the `sweep-status.json` sidecar (degraded sweeps PRESERVE it); `resolve_brief.check` demotes `⚠ resolve INCOMPLETE` → quiet `ℹ resolve steady-state … unchanged Nd` after ≥3 identical sweeps, but stays loud when degraded / below-threshold / no sidecar (anti-skip intact, `complete:False` held); gather.md + SKILL.md tell the model not to also raise a redundant System card. Root-caused live from the 2026-07-11 Dev brief (7/45 the same docs, 4th day). TDD 22/22 + 21/21, full suite green, live-verified on the real 45-task sweep; fresh-context review SHIP (folded in its MEDIUM SKILL.md gap + LOW int-guard + a reordering-stability test). (✅ 2026-07-11)
- [x] **A59** — Brief thread-reconciliation: the brief now reads `state/threads/` so worked items stop re-surfacing cold (complements A44's settle/Notion-write loop — same 2026-07-08 symptom, the thread-read side). Root-caused live from Seth's 2026-07-11 report (an awaiting-send item shown stale when already signed; a tax item shown "Not Started" when actually paid/in-progress; a notice-response item missing its waiting-on-mail history): `thread_id` was a cache-schema STUB — ad-hoc-populated, consumed by nothing; the durable `state/threads/` record was never read at gather. New `brief_threads.py annotate` (deterministic join — an `OI-N` thread owns only its own id, no capture-by-mention; slug threads own referenced OIs; never persists a derived `thread_id`) writes `in_motion {thread_id,status,next_action,court}`; `brief_render.py` splits ⚡ Act (your move, reframed to next_action) vs ⏳ In-motion (others=waiting / done=resolved; Act is the catch-all); wired into gather before `validate_cache`. Full gate: brainstorm→spec (ecosystem-check)→TDD (58 assertions)→fresh-context differential review caught+cleared a real HIGH (over-broad join HID a live item via a parked cross-ref) +2 MED +2 LOW; 45/45 green. Released **v0.4.1** (`f56a369`), build `7977e4b`. Caveat: synthetic-id items link only when the gather sets `thread_id` (gather-judgment, not pure determinism). (✅ 2026-07-11)
- [x] **A58** — Capture-router Windows long-path safety + dest-basename fit: `_longpath` (`\\?\` prefix for abs paths ≥260) wired into `_read_text`/`_read_json`/`_write_verify`/husk-delete, + `_fit_basename` caps routed dest names so the vault never receives a >260-char path. Root-caused live from the 2026-07-11 Dev brief — a 317-char Web-Clipper husk (whole tweet body as filename) was silently dropped `unreadable` → `ok:false` / daily `write errors` anomaly. Stuck file unblocked act-then-tell; engine fix TDD'd (104/104, incl. Windows >260 integration + `run()` idempotency) + fresh-context review verdict SHIP. (✅ 2026-07-11)
- [x] **A57** — Capture DEPTH (implements the unbuilt `inbox-capture` step-2 "Extract rich" contract): shared `url_extract` (markitdown URL, fail-soft; truststore for the TLS middlebox + browser-UA for article-site 403s) + `chrome_stub` enrichment wired into the nightly `aios-capture-router` (bookmarked articles now auto-enrich — live-verified, real Wikipedia 34k chars); env-ops YouTube-playlist lane (yt-dlp `-m` + `--no-check-certificates` + configurable cookies — live-verified end-to-end, 2.2k-char transcripts). **X article-follow REVERTED** (`bc03f27`) — headed-Playwright works but too fragile, twitter-cli audited-clean-but-not-adopted; X lane = the proven original scraper (see Watching). Full SDD: final opus review Ready-to-merge; the gate+live-verify caught+fixed 5 real bugs. aios suite 44/7/93. (✅ 2026-07-11; X reverted, decided)
- [x] **A41** — Autonomy-ratchet trust metric: **CLOSED won't-build (brainstorm w/ Seth 2026-07-10).** A solution to a problem not yet observed — the system hasn't been operated enough for the static `auto_ship_kbs` opt-in to have shown as friction, and there's no approval history to accumulate a "N-unchanged-in-a-row" signal from. The signal also doesn't exist today (the gate records `approved_by`, never unchanged-vs-modified), and the auto-widen form is the same moat-loosening pattern A55 retired (`auto_promote`); A9 (its "composes-with") is also dead. `auto_ship_kbs` stays a human opt-in. **Reopen trigger:** operating evidence that the static opt-in is real friction — e.g. repeatedly approving one KB's held items unchanged and wishing they'd auto-ship — at which point there's both a real problem and real data; build the ADVISORY form then (surface the streak, human ratchets — never system self-widen). (⛔ 2026-07-10)
- [x] **A56** — Ingest/distill DEPTH: engine built (SDD 8 tasks, merged `c45f36f`, suite 43, opus review clean) but **CLOSED not-wired** after the 217-stub backfill go/no-go DISCONFIRMED the thesis — 75 already-covered / ~7 minor genuine gaps / 42 concept-thin (capture-leak, not distill) / 89 reference; the real lesson is a dedup-before-flagging discipline + that the leak is at capture → A57. Engine stays merged/dormant, available if capture volume ever grows. Findings `docs/superpowers/findings/2026-07-10-a56-backfill-classification.md`; spec+plan in docs/superpowers (⛔-wire 2026-07-10)
- [x] **A42** (fact-leaks — number reused; the earlier ✅ A42 below is the v0.4.0 release) — Purge residual `dev`/`03_Dev` instance-name fact-leaks (3 cosmetic, none affect routing): (1) `brief_render._display` now acronym-aware — a short all-consonant slug uppercases (`gm`→`GM`), fixing the `Gm` title-case leak, while the profile `display_map` still wins and vowelled/long words title-case (new `_pretty` helper + test); (2) `capture_router_task._default` fallback `"dev"` → `sorted(kb_map)[0]` — derives a REAL kb from THIS install's map (also fixes a latent bug: the old "dev" could emit an invalid `--default-kb` on any install without a `dev` kb; +test); (3) genericized the `03_Dev` example paths in `skills/{brief,inbox-capture}/SKILL.md` → `<kb-folder>` placeholders. Deliberately LEFT (product-taxonomy, not "example paths"): the brief SKILL's `dev`-kb behavioral/scope-table refs — flag if you want those genericized too. fresh-context review Ready-to-merge (0 Critical/Important); suite green (test_brief_render 28, test_capture_router 15). build `42ed714`. (✅ 2026-07-10)
- [x] **A51** — Simplification cleanup: removed the dead one-time shard-collapse `queue_tx.migrate` verb (+ its CLI dispatch + usage-doc line; the G13 `queue.json.d/` layout was retired in A4). The `load()` shard-dir fence STAYS fail-loud + non-destructive — only its message repoints off the now-gone verb (downgrade to a pre-A4 tag to collapse a legacy shard dir). Updated 3 doc refs (`STAGE-CONTRACT.md`/`QUEUE.md`/`gate/SKILL.md`) + `test_queue_tx.py` (old migrate happy-path tests → one genuine fail-loud fence test) + removed a dead collateral `migrate()` call in `test_queue_tx_a13_draft_guard.py`; `rewind.py`'s unrelated live A28 `--migrate-retired` untouched. −104 lines; fresh-context review Ready-to-merge (0 issues, fence safety confirmed); suite green. This was the **one real item** of the retired "~2,300/20% removable" simplification thesis (garden = a re-schedule not a cut; deploy = working tested code). build `7b182b3`. (✅ 2026-07-10)
- [x] **A6** — Cross-OS scheduling (launchd / cron): Mac `deploy/mac/` launchd registrar/unregistrar/runner **live-round-trip-verified** on real macOS (2026-07-02); Linux `deploy/linux/` managed-crontab registrar/unregistrar/runner BUILT + reviewed (`1d2c248`). **Closed 2026-07-10** — the live-Linux round-trip won't happen without a Linux box; Linux code stands built+reviewed, live-fire DEFERRED → Watching. (✅ Mac live / ⏸ Linux-live deferred 2026-07-10)
- [x] **A12** — Declare + optionally wire the git dependency: the engine's undo is file-based (`ship.py` revert pointer / `rewind.py` snapshot) — NO engine git call — so the "dependency" was a **truth-in-docs gap**, not a missing dep. Softened the 2 over-claiming spots (`gate/SKILL.md`, `PIPELINE.md`) + added a README "undo needs no git" line; setup gained an opt-in Phase-4 `[y/N]` question + Phase-5 **instruct-don't-automate** git-init prompt (prints paste-ready commands, opt-in `Run now?`, idempotency/git-absent guards) — reusing the `new-business-unit` convention (surfaced by the plan-then-shop ecosystem check; earlier "automate git init" pick reversed). New hermetic guard test drives capture→ingest→real `ship`→real `undo_ship` on a non-git vault under a git-blocking PATH stub (9/9 green; implementer caught the plan's Windows stub was vacuous + fixed it; opus final review Ready-to-merge, 0 Critical/Important). Spec/plan `docs/superpowers/{specs,plans}/2026-07-10-a12-git-dependency*`; build `baf1d86`..`f7c79e5`. 3 non-blocking Minors → Watching. (✅ 2026-07-10)
- [x] **A35** — All-domain sweep + brief header "⚠ N economic figures with no paper" (A31 deferral, TDD). The overnight sweep is now **domain-attributed**: `flag_task` passes a `domain` through; `resolve_sweep_task` tags each figure by source db via a new `domain_group_map(env_root)` (`domain_groups[*].tasks_db` → group key) on the Notion path and by the task's own `domain` on the fixture path (item-own wins over source-default); `build_cache` emits per-flag `domain` + `no_paper_count` (flagged figures the crosswalk found NO candidate paper for, `candidates==[]`) + `no_paper_domains`. New `resolve_brief.economic_header(sweep)` → `{count, domains, line}` **recomputes** the count from `sweep['flagged']` (a pure function of the sweep — it can't render a number the sweep didn't produce; provably equals the sweep's own `no_paper_count`), fail-loud UNAVAILABLE when the sweep is missing (same discipline as `check`); new CLI `header` op wired verbatim into the headline card (`gather.md`). Proven live on a 3-domain fixture: sweep `no_paper_count=3` across {family_office, gm, lifeos} == header count 3. Fresh-context review (code-review + differential-review) zero-blocking; suite 42 green. (✅ 2026-07-10)
- [x] **A53** — `domain_mirror.py` full-silo migration gaps (3 engine capabilities, all TDD): (1) **cross-export relation** — an optional 4th flow-list arg names another export's `source_db` whose `url_to_slug` resolves the relation; `import_silo` pre-loads all mapped exports' slug maps; bare relations still same-export (back-compat); (2) **computed/state-native fields** — a declarative `computed_fields:` block with a closed `lookup` rule (map a field through a schema table, optional wikilink, default/fail-loud/None), evaluated after Notion-property fields, fact-free (specifics live in the silo schema); (3) **multi-line flow-folded scalars** in the SHARED `state_validate` reader (detect an unterminated quote, consume continuations raw, fold per YAML flow rules, re-encode single-line to reuse the decode path) — verified PyYAML-exact + round-trip on the real FO john-doe note. Built in an isolated worktree by a background agent; fresh-context review (deep scrutiny of the shared reader + 18 adversarial inputs) zero-blocking; finding #1 (cross-export `rel_source` to a non-mirrored db → pre-flight fail-loud, no partial write) fixed TDD on integration. Fixture `acme` silo proves genericity; shared validator PASS; fact-free grep clean; suite 41 green. Cherry-picked to main `8a64e6b`/`67135a0`/`a7af0f5`. (✅ 2026-07-10)
- [x] **A55** — Resolve-fate action: **retired the unwired `auto_promote`** from `resolve_verdict.compute_verdict` (all 6 branches) + its only readers (`gather.md` dossier field list, `test_resolve_e2e.py`, `test_resolve_verdict.py` — verdict assertions kept, 4 fns renamed off the retired concept) + the dead gate-promote BUILD-STATUS prose + the `SKILL.md:377` "auto_promote:true may fix-then-tell" instruction (→ "verdict is advisory; every economic promotion waits for approval"). `grep -rn auto_promote engine skills deploy` = zero; render_dossier (reads verdict/canonical/conflict/provenance) unaffected; suite 41 green; fresh-context review APPROVED zero findings. **The ~275-line "collapse" was REJECTED on inspection — third instance (after A50, A51(i)) of the deep-review's removable estimate not surviving the code:** `_read_resolve_list` is load-bearing (`_parse_yaml_subset` SKIPS lists — dropping it silently self-disables the sweep), `resolve_fetch` parses nested `links:` + list `aliases:` the flat reader can't touch, the warm-cache is intentional TESTED behavior, `seed_resolve_defaults` is a genuine tool. Only relocatable code was the test-only `template_keywords` (~18 lines, net-neutral) — not worth the churn. Paper-Governs unchanged. (✅ 2026-07-10)
- [x] **A38** — Scaled semantic-fallback self-heal: **closed won't-build 2026-07-10 (resolve-fate decision).** The multi-dir entity-discovery leg already shipped 2026-07-08 (`resolve_sweep_task` reads `entities_dir` as a list; test `test_build_cache_attaches_candidates_from_multiple_entity_dirs`); the remaining self-heal leg is feature-widening of the resolve subsystem, dropped under the subtraction stance. Reopen if a concrete need appears. (LOW follow-up if ever built: qualify duplicate-basename `entity` labels by dir.) (⛔ 2026-07-10)
- [x] **A36** — sync-trello resolve-at-capture: **closed won't-build 2026-07-10 (resolve-fate decision).** Feature-widening of the resolve subsystem (resolve at the source vs brief-time), dropped under the subtraction stance — the brief already resolves + surfaces the dossier at Seth's decision point. Reopen if a concrete need appears. (⛔ 2026-07-10)
- [x] **A54** — Gather-time capture no longer aborts on a session id-collision: capture ledger keys by payload_path, queue by stem, so a session already-in-queue re-discovered at a moved/unledgered path fell past both fences → `queue_tx add` hard-refused the dup id → whole run aborted (recurring gather wedge). Fix: `scan()` dedupes a `kind=="session"` stem-collision (session stem IS a unique id → same record) — skips re-enqueue, heals the ledger with the current path, counts `queue_collisions`, logs a `repair`; raws keep the loud never-silently-ledger contract; new `capture.py heal-ledger` reconcile back-fills the standing in-queue backlog in one pass. TDD reproduction (14 checks, reproduces the live abort then passes); fresh-context review (code-review + differential-review) zero blocking, no security regression; suite 40 green. MEDIUM follow-up (deduped-session downstream reachability) → Watching. (✅ 2026-07-10)
- [x] **A32** — env_root resolution hardening + setup anti-clobber guard (Cowork-subfolder failure): marker-first env_root resolve, never auto-`/aios:setup`, setup Phase-0 pre-flight clobber guard. Code shipped + 2× review-clean; Cowork validation moot — Cowork dead (Anthropic bug #69020) (✅ 2026-07-10)
- [x] **A9** — Per-object importance flags (below the category floor): closed won't-build — case is moot per own analysis (FO already `full` + omitted from `auto_ship_kbs`, so everything economic holds regardless); reopen if a real instance appears (✅ 2026-07-10)
- [x] **A45** — Domain-mirror engine module: generic fact-free `domain_mirror.py import` (Notion-export snapshot → `state/domains/<silo>/` records via a declarative `notion_fields:` schema map — coercions, deterministic emitter, config loader, driver; stdlib only, fact-free grep clean) delivered **import-only**. ≥2-silo genericity proven: a synthetic fixture `demo` silo + the real FO 5-table subset (entities/notes/people/insurance/tax-ledger) semantic-equiv via the engine's OWN reader vs the live migrators. Root-caused a shared defect: `state_validate` reader now decodes YAML escapes (quote-style-aware, strictly additive, 44/44). 5 covered FO migrators retired → pointer README. `publish` + render-views + the remaining ~16 cross-export/computed/flow-folded FO tables deferred → **A53**; Personal silo is H25; render-views is #4. Brainstorm→spec→plan→SDD (5 tasks, per-task + opus whole-branch review clean, 0 Critical/Important); merged main `b780be3` + cross-repo (claude-env `37b6b41`, family-office `4876cf9`/`e530a7d`, gm `67d9654`). Spec/plan `docs/superpowers/{specs,plans}/2026-07-09-a45-domain-mirror*` (✅ 2026-07-09)

- [x] **A52** — Context-log Windows concurrent-append line loss (~4% under 4-way concurrency): **ACCEPTED / wontfix.** Observability-only (queue.json stays safe via its own lock + tmp/replace), never tears a line / never raises, self-healed by the A21 `check` stage-presence backstop. Chosen over a lock-reuse or `FILE_APPEND_DATA` fix (2026-07-09 brainstorm, Seth) — minimal-surface, no new code. The real defect fixed was the capability-truth violation: `emit()`/`_default_append` docstrings now state POSIX-atomic (`O_APPEND`) / Windows-NON-atomic honestly instead of over-claiming cross-platform atomicity. Full rationale in the docstrings + this line's closing commit — a future session that "discovers" the Windows drop should read here before re-opening it. (✅ accepted 2026-07-09)
- [x] **A50** — truth-in-docs: the product no longer ships prose asserting unbuilt capability. (1) `PIPELINE.md` self-improvement claim downgraded to design-intent (receiving rail exists — gate holds `source:self` — but NO producer mines the context-log; matches `lane_policy.py:16`'s honest label); (2) `inbox-capture/SKILL.md` x/github/youtube/whatsapp adapters + `native` pull-mode marked DESIGN INTENT / not built (only `reuse` globbing + `sessions` + session-run MCP email/drive are real); (3) resolve dead-ends flagged in `resolve_verdict.py` + the spec (`auto_promote` consumed by nothing, `resolve.js` not on disk — re-home is A51). (4) **brief SKILL: consolidated each enforcement rule to ONE canonical `# Invariants` block + collapsed the inline repeats to pointers + removed the drift-prone "Render format" mirror table** — fresh-context review confirmed ZERO enforcement rules lost. **NOTE on the `≤300 lines` target: not met and it was an overestimate** — the "~250 removable lines" premise was wrong (the repetition is ~1 line each ≈ 20 lines total; the file's 568 lines are dominated by IRREDUCIBLE unique procedure — two-pass render, cache contract, walk ledger, scope table, settle write-ordering, grading, phase-A draft-path resolution, batch panel). Cutting to 300 would delete load-bearing procedure from the daily driver; prioritized the real bar ("each rule once, enforcement intact") over the number. review APPROVED (✅ 2026-07-09)
- [x] **A40** — `resolve_verdict` CLI now distinguishes `paper_sources` absent/None (default `{drive}`) from an explicit `[]` ("trust nothing" → no paper-tier promotion); the old `... or ["drive"]` collapsed both. CLI-driven test (empty → verbal-only, absent → papered+auto-promote, custom-set-excluding-drive → verbal-only); review APPROVED; suite 39 (✅ 2026-07-09)
- [x] **A39** — `resolve_fetch.candidates_for` now UNIONs the nested `links.aliases` block with the Obsidian-native top-level `aliases:` key (new `_top_level_aliases`, inline + block-list, column-0 only so a nested key isn't double-read), order-preserving + deduped — so an entity no longer needs a duplicate `links.aliases`. +4 tests; review APPROVED; suite 39. **Env-ops follow-up:** drop the Bayview entity's now-redundant `links.aliases` duplicate in the live SecondBrain vault (separate repo). (✅ 2026-07-09)
- [x] **A49** — review-surfaced correctness batch (6 bugs, each silent-until-it-bites): (1) resolve permanent partial-source rot — `resolve_sweep_task` now writes a `sweep-status.json` freshness sidecar (consecutive-degraded streak + last-good), and `resolve_brief.check`/`_staleness_line` surface a loud `⚠ resolve sweep DEGRADED` line so a permanently-failing source can't freeze the warm cache while looking fresh; brief SKILL lifts it; (2) cp1252 raw permanent stall — `errors="replace"` on the two CONTENT readers (`capture._read_text`, `sort._read_text`; never queue/ledger); (3) brief scope case-leak — `resolve_scope` uses `os.path.normcase` (case-insensitive match on Windows, identity on POSIX); (4) `capture._frontmatter` nested-YAML misread — now skips indented lines by leading char + space-key guard (matches the sibling readers); (5) `brief_render` unguarded `item["title"]` → `.get(...) or "(untitled)"` (2 spots); (6) `normalize_url` order-insensitive dedupe (`sorted(kept)`). +15-check regression batch (`test_a49_correctness_batch.py`); fresh-context review APPROVED (1 LOW SKILL-prose nit, fixed); suite 39 green (✅ 2026-07-09)
- [x] **A48** — gate moat hardening (two fail-safe floors, moat core untouched): (HOLE A) `scheduled_ship_action` now REQUIRES a scannable body — a skipped/failed draft-excerpt enrichment defers to hold instead of shipping past a blind tripwire (`_has_scannable_body`, isinstance-str so a `null` body reads as no body — a bug the review-gate caught); (HOLE C) new `manual_ship_action` = `ship_action` + advisory economic tripwire, so an economic-smelling auto-ship/past-TTL-confirm item surfaces for explicit approval even in a cleared KB — the manual gate skill AND task body (`deploy/tasks/gate.md` run step, not just prose — 2nd review-gate finding) now call it; both variants single-sourced via `_ship_with_economic_floor(require_body)` to kill drift; confirm-wording reconciled. review-gate PASS + focused re-review APPROVED (both caught real gaps: the null-body bypass + incomplete task-body wiring, fixed). +8 tests (39 lane_policy); suite 37 green (✅ 2026-07-09)
- [x] **A47** — state-core concurrency hardening: (1) `_Lock` stale-reclaim TOCTOU closed — reclaim now atomically takes ownership (rename-to-unique) and decides staleness on the OWNED inode, so a live lock re-created in the old check→act window can never be stolen (the 2-waiter double-hold is provably single-winner; a documented 3-waiter restore-window residual remains, inherent to advisory locks w/o CAS); (2) `context_log.emit` false-corruption alarm removed — dropped the post-append additive readback that raised on data safely on disk under concurrent O_APPEND, kept the torn-tail guard; (3) parent-dir `fsync` added to `_write_atomic` (no-op on Windows). review-gate PASS then a focused re-review APPROVED the rework (both flagged only documented residuals + a vacuous test glob, fixed). Deterministic reclaim tests (live lock never removed) + interleave/concurrency tests; suite 37 green. Deferred: `migrate()` deletion → A51 (broad cross-file refs = blind-cut). New finding → seed A52 (✅ 2026-07-09)
- [x] **A46** — setup VERIFY gate no longer hardcodes the scheduled-task count (was `5`, real manifest has 7 enabled-native → a correct fresh install falsely reported VERIFY failure): new checked-in `task_manifest.py` derives the enabled-native count/ids from `tasks.manifest.json` with the registrars' exact filter; win32/mac/linux step-4 + the VERIFY block + `docs/C8-TEST.md` all now derive N (adding a task can't re-break the gate). Registrar dry-run shows 7 = derived; test proves the 8th-task case. Fresh-context review caught 2 platform lines I'd missed (fixed). suite 37 green (✅ 2026-07-09)
- [x] **A44** — **Settle loop: closes the Notion write-loop + reliable self-monitoring precompute.** Root cause (2026-07-08 stale-brief symptom): Notion (operational SoR) only healed via manual brief-walk clicks, so out-of-band completions drifted stale and the brief re-served done work; the precompute silently died on `max_turns` (`run-hidden.vbs` swallowed the inner exit code → Task Scheduler saw `0x0`). Shipped (Phase A/B/C): Stage-0 **settle station** — deterministic auto-heal fix-then-tell (`settle_reconcile.py`, ledger↔changelog diff of unlanded intended writes) + gated inferred confirm → `notion_writeback flip` → `record_decision --notion-write` (the loop that keeps Notion current); `settle` cache block + `validate_cache` gate + scope-filtered `render_settle`; precompute enabled + `max_turns` 80→150 + live-Notion leg verified (`notion_live:true`); `run-hidden.vbs` exit-code propagation + `brief_freshness_check` anomaly alarm; gate-header fix (ships wiki, not Notion). Subagent-driven 11-task build; whole-branch review caught a CRITICAL (freshness alarm emitted `anomaly` vs `pipeline_health`'s `anomalies` list — silently dead) + IMPORTANT scope-leak, both fixed w/ regression tests; suite 34 green. Spec/plan `docs/superpowers/{specs,plans}/2026-07-08-settle-loop*`; build `b85eae8..3b29a17`. 6 non-blocking Minors logged for a future polish pass (✅ 2026-07-08)
- [x] **A43** — `ship.py` daily-note merge no longer duplicates a full-note husk: new `_draft_supersets` → REPLACE-on-superset (the draft already contains the whole incumbent) instead of appending; delimited append kept for genuine deltas. TDD test 4b (husk → exactly one H1, body once, new `### Added` section folded, revert-safe). Fresh-context review zero CRITICAL/HIGH (3 non-blocking notes; docstring softened). test_ship 31 + suite 35 green (✅ 2026-07-08)

- [x] **A42** — **v0.4.0 released** so A31/A33/A34 reach the interactive brief: `plugin.json` 0.3.0→0.4.0, tagged `v0.4.0`, pre-tag gate passed (suite 32, sanitize sweep, install-break skim); Seth ran `/plugin update` → installed snapshot now carries `resolve_brief.py` + the PASS-2 wiring (3 refs) and the SHIPPED tools fire on the real cache (`check` → `45 of 45` loud, `render_dossier` conflict card). **Public-repo history SCRUBBED** (git-filter-repo): real Notion ids + economic figures + the Drive id/doc removed from ALL history (0 occurrences verified), fixtures re-faked, force-pushed main + tags; backup bundle in the session scratchpad (✅ 2026-07-07)
- [x] **A37** — SUBSUMED by A31: `brief_render.render_dossier` emits the verbatim papered/conflict/verbal-only/silent card (verdict→card, model supplies data only), unit-tested per verdict; no hand-written verdict line remains in brief prose (✅ 2026-07-07, in A31 `6d64ca4`)
- [x] **A31** — task resolution layer COMPLETE: the resolve step now FIRES in the brief off the A34 warm cache. `resolve_brief.py` (worklist + presence/staleness/identity `check` — fail-loud on missing sweep, `_safe_id` traversal-safe, collision-guarded) + `brief_render.render_dossier` (verbatim papered/conflict/verbal-only/silent card = A37 subsumed) + `write_dossier` (sanitized, sweep_hash-stamped); gather.md rewired worklist-driven + SKILL.md PASS-2 lifts the cards + the ⚠-INCOMPLETE line verbatim. Subagent-driven 6-task build, whole-branch review-gate PASS, 6 findings + 1 re-review residual all fixed & verified; suite 32, resolve_brief 17, render_dossier 7. Slice-1 engine/verdict layer shipped earlier (`cc4d645`..`66a1190`). Spec `58c8e03`/plan `aa96a57`; build `d641485`..`d85d205` (✅ 2026-07-07)

- [x] **A34** — overnight resolve pre-sweep SHIPPED + registered + proven live: `resolve_sweep_task.py` (type:script; profile resolve-config → headless `notion_gather` over `resolve.task_source_dbs` data-source ids → `resolve_sweep` flag → word-boundary `resolve_fetch` crosswalk attach → hashed warm cache → context-log; degrade-safe on any failed/partial gather — a HIGH + its partial-outage variant caught across TWO review-gate passes and fixed; atomic write; inline+block keyword read), manifest `aios-resolve-sweep` (opt-in, cron 30 6), 17 tests; registered on real Task Scheduler and the scheduled run gathered 71 live Notion tasks → 45 flagged → warm cache written (token reachable in scheduler context); unregister clean. Leg 2 ("brief reads warm cache") is A31's brief-wiring, not this item — rerouted (✅ 2026-07-07, `2581922`+`08a71c7`)
- [x] **A33** — setup seeds default `resolve:` config so the resolution layer isn't inert on fresh installs: a verbatim-lifted `engine/templates/resolve-defaults.yaml` + an idempotent, Refresh-safe, stdlib-only `seed_resolve_defaults.py` (checked-in tool, not skippable prose — the A31 lesson) invoked from setup Phase 5; fact-free; review-gate PASS + elegance pass (noisy-substring keyword prune, decomment-safe parse, non-vacuous newline test); 9 new tests, suite green (✅ 2026-07-07, `8931135`)
- [x] **A30** — gate ship husk-leak fixed: `ship.py` retires the staging draft after the canonical write is confirmed (move → revert dir, fail-closed AFTER the queue flip so a torn ship never strands a draft), `rewind.py undo_ship` restores it (backward-compat, no-clobber); 4 new regression checks assert staging/canonical disjoint + revert round-trip; fresh-context review caught + fixed a torn-ship ordering regression; 27 ship + 24 suite green (✅ 2026-07-06)
- [x] **A7** — tactical Notion write-back as a fenced engine tool (`notion_writeback.py`: allowlist + pause_economic tripwire + receipt/read-back + typed-only); live test-db acceptance shown; vacuous-pass suite hole fixed; production rollout stays profile-gated (✅ 2026-07-05, `d9ac702`)
- [x] **A28** — `retired: true` lifecycle marker + `mark-retired` op + evidence-based `reconcile --migrate-retired`; 16 live evidenced cases cleared, 9 residuals → Watching (✅ 2026-07-05, `4580be5`)
- [x] **A3** — Benos rulebook harvested into garden Steps 1–3: `garden_hygiene.py` mechanical oracle + vendored/adapted rulebook + tiered auto-ship/review lanes; design doc recovered from deleted harness repo via transcript (✅ 2026-07-05, `aa88e62`; Sunday watch above)
- [x] **A29** — capture queue backstop fence: the add-then-ledger-fail tear self-heals; exact-identity match only (✅ 2026-07-05, `1b315b1`)
- [x] **A27** — dropped the double-registered `commands/{brief,gate,setup}.md` wrappers; `pipeline.md` kept (✅ 2026-07-05, `34b02af`)
- [x] **A26b** — capture: URL-dupes' stable ids ledgered at fence time; ledger ⊇ queue ids by design (✅ 2026-07-05, `34b02af`)
- [x] **A11** — Act-vs-Track overview renders via `brief_render.py overview`; legacy `{layer,action}` shape handled ungraded (✅ 2026-07-05, `3cdf00f`)
- [x] **A15** — review-lane age line (`held-summary`) + batch-panel grouping, verbatim-lifted; engine legs (✅ 2026-07-05, `3cdf00f`)
- [x] **A21** — `context_log.py check` runner backstop + dated result rotation; both damaged live log lines repaired (✅ 2026-07-05, `24af378`)
- [x] **A23** — vanishing staging drafts root-caused (A19 sweep live-fired pre-review); sweep liveness = fail-closed set-membership; victims repaired (✅ 2026-07-05, `e64e6b4`; nightly watch above)
- [x] **A20** — headless read-only git for fleet runs: conditional runner git line + invocation form (✅ build legs 2026-07-05; validation = Watching)
- [x] **A24** — tagged **v0.2.0** (pre-tag gate: suite green, sanitize sweep, install-break skim) (✅ 2026-07-05)
- [x] **A26a** — deploy-body dedup to pointer style + fact-free leak fixes + prose diet, −316 lines (✅ 2026-07-05)
- [x] **A4** — queue collapsed to single `queue.json`; FUSE defense ripped engine-wide, −214 lines; instance migrated 856→1 (✅ 2026-07-05)
- [x] **A5** — harness→aios vocabulary polish, L3–L9 close-out (✅ 2026-07-05, `11d76cb`)
- [x] **A25** — five checked-in engine tools (`ship/sort/cache-status/setup_render/capture_router_task`) replace nightly improvisation; `--max-turns`; script-type tasks (✅ 2026-07-05; bleed watch above)
- [x] **A18** — headless Notion read (`notion_gather.py` + capability-parity guard); verified live unattended (✅ 2026-07-05; cron watch above)
- [x] **A22** — ➡ moved to env-ops as **H33** (instance-data migration, closed there 2026-07-05)
- [x] **A19** — live-vault orphan sweep + reconcile kb-map resolution + long-path shim; 294-orphan triage (✅ 2026-07-04, `a9992a0`)
- [x] **A17** — brief SKILL reality-sync, live-gather as happy path, RESOLVE-INSTALL.md dedup (✅ 2026-07-04)
- [x] **A16** — machine-run marker: fleet runs stamped + excluded from session-capture (✅ 2026-07-04, validated 07-05)
- [x] **A13** — ingest "no draft found" bug: drafted-before-awaiting guard + `draft_path`; 48 victims rewound (✅ 2026-07-04)
- [x] **A14** — `sorted` pile drained 323→0 (13-agent fan-out); throttle 8→25; rejected-pile triage (✅ 2026-07-03)
- [x] **A8** — Seth's env re-based onto the aios plugin (state/profile re-home, task fleet swap); tails closed via H34/A19 (✅ 2026-07-02)
- [x] **A10** — deterministic brief card render (`brief_render.py`); the 🔵/🟠 block can't drop (✅ 2026-07-02, `66fd37b`)
- [x] **A2** — distill-retire garden ported from frozen harness; retire leg wired to the gate; **v0.1.1** (✅ 2026-07-02, `f7e89f7`)
- [x] **A1** — `shmattox/aios` flipped public; marketplace install verified (✅ 2026-07-02)
- [x] **C1–C9** — plugin scaffold, engine+skills port, deploy layer, setup, dress rehearsal, final review; **v0.1.0** (✅ 2026-07-01)
