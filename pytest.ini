# A133 — pin collection to the engine suite so a non-dot worktree/copy dir under the
# repo root can never double-collect the whole tree (pytest's default `.*`
# norecursedirs only skips dot-dirs like .worktrees/ and .superpowers/).
[pytest]
testpaths = engine/tools/tests
addopts = -q
